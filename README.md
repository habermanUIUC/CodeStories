# CodeStories-lessons
This is the public facing repo to hold lessons that will be copied in
Create a repo via github (used authy for 2FA)
Create an access token ( via settings->developer)
copy the access token


echo "# CodeStories-lessons" >> README.md
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/habermanUIUC/CodeStories-lessons.git
git push -u origin main
<request for userName/access token>

